services:
  valkey:
    image: valkey/valkey:8.1.2
    container_name: valkey-stream-server
    ports:
      - "6379:6379"
    volumes:
      - valkey_data:/data
    restart: always

volumes:
  valkey_data: